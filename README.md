# selenium-project

Default repository setting
Repository Settings
Settings
Actions → General
③ 하단으로 스크롤 → Workflow permissions
여기서 반드시 아래처럼 설정해야 한다:
(●) Read and write permissions
[✓] Allow GitHub Actions to create and approve pull requests
④ Save

3. GitHub Pages 브랜치 설정
① Settings → Pages
② Source
Branch: gh-pages
Folder: / (root)
④ Save
